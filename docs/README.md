# HAMS-BAP Docs

HAMS-BAP 프로젝트 문서 루트다.

## 구성

- `pm/`
  - 프로젝트 개요, 기능 맵, 운영 체크리스트
- `job/`
  - 날짜별 작업 로그와 구현 변경사항
- `pages/`
  - `app/**/page.tsx` 기준 화면 문서

## 문서 원칙

- 페이지 문서는 실제 URL 기준으로 정리한다.
- 라우트 그룹명 `(content)`, `(content-header)`, `(siderbar-header)` 은 URL에 반영하지 않는다.
- 작업 로그는 날짜 폴더 아래에 주제별 Markdown 파일로 기록한다.

## 현재 기준

- 기준 저장소: `hams-BAP`
- 기준 일자: `2026-03-12`
- 페이지 문서 범위: `app/**/page.tsx`

## 작업 로그

- `job/20260311/builder-react-flow-ui.md`
  - `/admin/builder` React Flow UI 개선 작업 정리
